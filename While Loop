#include <stdio.h>

int main() {
    int number;
    printf("enter a number: ");
    scanf("%d", &number);
      int i=0;
    while (i <= 20) {
       
         int P=i * number;
         printf("%d * %d = %d\n", number, i, P);
         i=i+1;
        }
    return 0;
}

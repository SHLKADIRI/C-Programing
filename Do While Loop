#include <stdio.h>

int main() {
    
        int num;
        printf("enter a number: ");
        scanf("%d", &num);
        int i=0;
    do {
        int p=i*num;
        printf("%d * %d = %d\n", num, i, p);
        i=i+1;
    } while (i <= 20);

    return 0;
}

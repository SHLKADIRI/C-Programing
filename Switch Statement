#include <stdio.h>

int main() {

  int number;
  printf("Enter a number between 1 to 2: ");
  scanf("%d", &number);

  switch(number) {
    case 1:
      printf("Sunday");
      break;

    case 2:
      printf("Monday");
      break;

    default:
      printf("Invalid Number");
  }

return 0;
}
